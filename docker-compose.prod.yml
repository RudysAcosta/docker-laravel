version: '3.8'

services:
  nginx:
    build:
      context: .
      dockerfile: .cloud/nginx.prod.dockerfile
    ports:
      - 80:80
      - 443:443
    depends_on:
      - mysql
      - php
  php:
    build:
      context: .
      dockerfile: .cloud/php.prod.dockerfile